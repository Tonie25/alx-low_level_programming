#include "main.h"

/**
 * print_alphabet-x10 -> prints lowercase alphabets
 */

void print_alphabet_x10(void)
{
	int k;
	int w;
	
	for (k = 0; w < 10; k++)
	{
		for (k = 'a' ; w <= 'z' ; k++)
		{	
		      _putchar(k);
		}
	}
	_putchar('\n')
}

